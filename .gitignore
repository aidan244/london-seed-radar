# Secrets: never commit. The Companies House key lives here.
.env

# Working database: source of truth lives locally, public dataset is exported to docs/data/
radar.db
radar.db-journal
radar.db.bak-*

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# OS
.DS_Store

# Local working notes, not for the public repo
HANDOVER.md

# Local progress dashboard; surfaces internal working state
# (dropped candidates, drop reasons, todos). Never publish it.
dashboard.html

# Generated paste artifact, recreate with python -m radar.paste
issues/*/substack-paste.html
