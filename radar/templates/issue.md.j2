# 📡 London Seed Radar · UK, London first · issue of {{ issue_label }}

**{{ subtitle }}**

> DRAFT. Edit before publishing; the voice below is scaffolding, not the
> final copy. Re-check every claim against the evidence links. Amounts
> from filings are best-effort: an SH01 proves a raise happened, not how
> big it was.

[IMAGE: this issue's masthead. Edit the issue-line text (and blip
count, if you care) in docs/assets/masthead.svg, export per "Banner
export" in reports/design/newsletter-design-notes.md, insert
the PNG here.]

[INTRO: two or three sentences in your own voice about the fortnight.]

{% if shortfall_note %}
{{ shortfall_note }}

{% endif %}
This fortnight: {{ entries|length }} newly funded early-stage companies across the UK, London first.

---

## ⚡ THE FORTNIGHT AT A GLANCE

{% for entry in entries %}
{{ loop.index }}. {{ entry.glance_label }}
{% endfor %}

*🟢 hiring now · ⚪ no live roles found · 🔒 job board unverifiable ·
🎓 a role a recent grad could apply for · 🌐 a role that can be done
remotely. 🎓 and 🌐 are flagged from the posting text only.*

{% if headline_label %}
> {{ headline_label }}
> [SUBSTACK: format this as a pull quote]

{% endif %}
{% for entry in entries %}
---
{% if entry.section_heading %}

# {{ entry.section_heading }}
{% endif %}

## {{ loop.index }} · {{ entry.company.name }} {{ entry.badge }}

**{{ entry.strapline }}**

{{ entry.company.one_liner or "[TODO: one-liner]" }}

*Why it matters: [one line on why a reader should care; cut it if you have nothing].*

{% if entry.founders_label %}
- 👥 **Founders:** {{ entry.founders_label }}
{% endif %}
- 📊 **Team:** {{ entry.team_label }}
- 📈 **Hiring:** {{ entry.hiring_label }}
- 🧾 **Evidence:** {{ entry.evidence_label }}

{% endfor %}
---

## 📬 BEFORE YOU GO

[OUTRO: your sign-off, in your own voice.]

**Spotted an error?** Reply to this email; corrections run in the next
issue. If you are a founder featured here, replies are especially
welcome.

[SUBSTACK: insert Subscribe and Share buttons here]

📂 **Dataset and archive:** every company, every fortnight, as JSON and CSV
on the [archive site](https://aidan244.github.io/london-seed-radar/).

---

## 🛠️ PASTE CHECKLIST (python -m radar.paste strips this section)

1. Edit the copy above; rewrite or cut every "Why it matters" line.
2. Check every 🎓 and 🌐 against the live posting; both are guessed from
   the posting text alone. Remove wrong ones, add missed ones.
3. Replace the [IMAGE] placeholder with the exported banner
   (docs/assets/masthead-2200x440.png renders sharpest in email).
4. Run: python -m radar.paste --copy (puts the rendered rich text on
   the clipboard).
5. Open a new Substack post and paste. If the clipboard route fails,
   open the substack-paste.html it wrote, select all, copy, paste.
   Raw markdown does not paste; rendered rich text does.
6. Convert the "largest round" blockquote into a pull quote; insert
   Subscribe and Share buttons where marked.
7. Subject line starts with 📡; preheader is the subtitle line.
8. Mobile preview: if at-a-glance lines wrap badly, shorten the
   descriptors, not the badges.
